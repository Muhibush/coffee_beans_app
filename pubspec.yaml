name: coffee_beans_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.2.0
  google_fonts: ^8.0.2
  url_launcher: ^6.3.2
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  flutter_web_plugins:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
